enum PaymentStatus { pending, partial, paid }
enum BatchType { meat, layer }
enum BirdOrigin { purchase, incubation }
enum BatchStatus { active, closed }
enum ExpenseType { feed, vaccine, medication, labor, energy, custom }
enum EggSize { extra, large, medium, small }
enum EggUnit { dozens, trays30 }
enum ChickenSaleType { live, slaughtered }
enum PartnerType { chickSupplier, feedSupplier, veterinarian, slaughterhouse, other }
