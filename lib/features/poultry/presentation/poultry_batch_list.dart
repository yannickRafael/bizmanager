import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 4.
class PoultryBatchList extends StatelessWidget {
  const PoultryBatchList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐔 Lotes de Aves')),
      body: const Center(child: Text('Poultry Batch List — Em migração')),
    );
  }
}
