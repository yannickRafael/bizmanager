import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 4.
class PoultryDashboard extends StatelessWidget {
  const PoultryDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐔 Aves — Dashboard')),
      body: const Center(child: Text('Poultry Dashboard — Em migração')),
    );
  }
}
