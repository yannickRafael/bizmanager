import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 4.
class PoultrySalesScreen extends StatelessWidget {
  const PoultrySalesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐔 Vendas de Aves')),
      body: const Center(child: Text('Poultry Sales — Em migração')),
    );
  }
}
