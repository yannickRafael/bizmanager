import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 6.
class GoatSalesScreen extends StatelessWidget {
  const GoatSalesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐐 Vendas de Caprinos')),
      body: const Center(child: Text('Goat Sales — Em desenvolvimento')),
    );
  }
}
