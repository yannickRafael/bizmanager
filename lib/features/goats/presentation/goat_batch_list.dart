import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 6.
class GoatBatchList extends StatelessWidget {
  const GoatBatchList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐐 Rebanhos')),
      body: const Center(child: Text('Goat Batch List — Em desenvolvimento')),
    );
  }
}
