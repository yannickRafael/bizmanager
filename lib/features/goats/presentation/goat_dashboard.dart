import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 6.
class GoatDashboard extends StatelessWidget {
  const GoatDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐐 Caprinos — Dashboard')),
      body: const Center(child: Text('Módulo de Caprinos em desenvolvimento.')),
    );
  }
}
