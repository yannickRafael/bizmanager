import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 5.
class CattleBatchList extends StatelessWidget {
  const CattleBatchList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐄 Manadas')),
      body: const Center(child: Text('Cattle Batch List — Em desenvolvimento')),
    );
  }
}
