import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 5.
class CattleSalesScreen extends StatelessWidget {
  const CattleSalesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐄 Vendas de Bovinos')),
      body: const Center(child: Text('Cattle Sales — Em desenvolvimento')),
    );
  }
}
