import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 5.
class CattleDashboard extends StatelessWidget {
  const CattleDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('🐄 Bovinos — Dashboard')),
      body: const Center(child: Text('Módulo de Bovinos em desenvolvimento.')),
    );
  }
}
