import 'package:flutter/material.dart';

/// Placeholder — will be fully built in Phase 4.
class FarmsScreen extends StatelessWidget {
  const FarmsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Explorações')),
      body: const Center(child: Text('Explorações — Em migração')),
    );
  }
}
